version: 0.0
os: linux
files:
  - source: HelloWorld-1.war
    destination: /var/lib/tomcat10/webapps
    overwrite: yes
permissions:
  - object: /var/lib/tomcat10/webapps/HelloWorld-1.war
    owner: tomcat
    group: tomcat
    mode: 644
